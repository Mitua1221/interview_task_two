package a

class NameFormatting () {

// We get a names/surnames examples from android phonebook via broadcast receiver.

// We need to make an Initials from this names and surnames.

// Examples: "Denis Lee" -> "DL", "Mark Zuckerberk" - "MZ", "Yunis Abdulzade" - "YA".

// Remember that initials are the first two letters of first and last name, so remember about corner-cases.

    // return initials
    fun getInititals(nameSurname: String): String {
        return ""
    }

}